function scrollToMatches() {
  var matchesBlock = document.getElementById("matches-news");
  if (matchesBlock) {
      matchesBlock.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
      });
  }
}
function scrollToMatches() {
  var matchesBlock = document.getElementById("teams");
  if (matchesBlock) {
      matchesBlock.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
      });
  }
}